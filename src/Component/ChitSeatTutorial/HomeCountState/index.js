import React from 'react';
import Counts from '../Counts/Counts';

const HomeCountState = () => {
    return (
        <div>
           <Counts/> 
        </div>
    );
};

export default HomeCountState;